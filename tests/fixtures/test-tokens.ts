/**
 * Test tokens for use in unit and integration tests.
 * These tokens represent mock ports for testing adapter behavior.
 */

export const TEST_STORAGE_TOKEN: unique symbol = Symbol('TEST_STORAGE_TOKEN')
export const TEST_API_TOKEN: unique symbol = Symbol('TEST_API_TOKEN')
export const TEST_CACHE_TOKEN: unique symbol = Symbol('TEST_CACHE_TOKEN')

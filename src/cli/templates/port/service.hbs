import { Injectable } from '@nestjs/common'
import { InjectPort } from '{{coreImportPath}}'
import type { {{namePascal}}Port } from './{{fileName}}.port'
import { {{nameScreamingSnake}}_{{portSuffix}} } from './{{fileName}}.token'

@Injectable()
export class {{namePascal}}Service {
	constructor(
		@InjectPort({{nameScreamingSnake}}_{{portSuffix}})
		private readonly {{nameCamel}}: {{namePascal}}Port,
	) {}
}

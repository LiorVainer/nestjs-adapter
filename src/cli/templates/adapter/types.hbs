/**
 * {{namePascal}} Adapter Types
 *
 * This file defines the configuration options for the {{namePascal}} adapter.
 * These options are infrastructure-specific and should NOT be exposed to the domain layer.
 */

/**
 * Configuration options for the {{namePascal}} adapter.
 */
export interface {{namePascal}}Options {
	/**
	 * TODO: Add configuration options here
	 *
	 * Example:
	 * apiUrl: string
	 * apiKey?: string
	 * timeout?: number
	 */
}
